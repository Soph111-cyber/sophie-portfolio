function openSectionEditor(id){editingSectionId=id;const s=data.sections.find(x=>x.id===id);if(!s)return;$('editorHeading').textContent='Edit: '+s.label;$('secLabel').value=s.label||'';$('secIcon').value=s.icon||'';$('secTitle').value=s.title||'';$('secSubtitle').value=s.subtitle||'';$('secOpacity').value=s.opacity??.48;$('secOverlay').value=s.overlay??.4;$('secBgPreview').style.backgroundImage=s.bg?`url("${s.bg}")`:'';renderBlockEditor(s.blocks||[]);$('sectionEditor').showModal()}
$('closeSectionEditor').onclick=()=>$('sectionEditor').close();$('secBgUpload').onchange=async e=>{const s=data.sections.find(x=>x.id===editingSectionId),f=e.target.files[0];if(!s||!f)return;s.bg=await compressImage(f,1800,.82);$('secBgPreview').style.backgroundImage=`url("${s.bg}")`;e.target.value=''};$('clearSecBg').onclick=()=>{const s=data.sections.find(x=>x.id===editingSectionId);if(s)s.bg='';$('secBgPreview').style.backgroundImage=''};
function renderBlockEditor(blocks){const list=$('blockEditorList');list.innerHTML='';blocks.forEach((b,i)=>{const el=document.createElement('div');el.className='block-edit';el.dataset.index=i;let fields='';if(b.type==='text'||b.type==='quote')fields=`<textarea data-field="text">${esc(b.text||'')}</textarea>`;if(b.type==='image')fields=`<img class="preview-img" data-preview src="${esc(b.url||'')}" style="${b.url?'':'display:none'}"><input data-field="url" type="hidden" value="${esc(b.url||'')}"><div class="inline-grid"><label class="upload-btn">Upload / replace photo<input data-image-upload type="file" accept="image/*" hidden></label><input data-field="caption" value="${esc(b.caption||'')}" placeholder="Caption"></div>`;if(b.type==='gallery')fields=`<div class="gallery-admin-preview" data-gallery-preview></div><textarea data-field="images" hidden>${esc((b.images||[]).join('\n'))}</textarea><div class="inline-grid"><label class="upload-btn">Add photos<input data-gallery-upload type="file" accept="image/*" multiple hidden></label><label>Columns<select data-field="columns"><option ${b.columns==2?'selected':''}>2</option><option ${b.columns==3?'selected':''}>3</option></select></label></div><button class="small" data-clear-gallery>Clear gallery</button>`;el.innerHTML=`<div class="block-edit-top"><strong>${b.type.toUpperCase()}</strong><div><button class="small" data-up>↑</button><button class="small" data-down>↓</button><button class="small" data-remove>Remove</button></div></div>${fields}`;el.querySelector('[data-up]').onclick=()=>moveBlock(i,-1);el.querySelector('[data-down]').onclick=()=>moveBlock(i,1);el.querySelector('[data-remove]').onclick=()=>removeBlock(i);const iu=el.querySelector('[data-image-upload]');if(iu)iu.onchange=async e=>{const f=e.target.files[0];if(!f)return;const src=await compressImage(f,1600,.82);el.querySelector('[data-field="url"]').value=src;const im=el.querySelector('[data-preview]');im.src=src;im.style.display='block';e.target.value=''};const gu=el.querySelector('[data-gallery-upload]');if(gu){renderGalleryAdmin(el);gu.onchange=async e=>{const area=el.querySelector('[data-field="images"]'),existing=area.value.split('\n').filter(Boolean);for(const f of [...e.target.files])existing.push(await compressImage(f,1300,.8));area.value=existing.join('\n');renderGalleryAdmin(el);e.target.value=''};el.querySelector('[data-clear-gallery]').onclick=()=>{el.querySelector('[data-field="images"]').value='';renderGalleryAdmin(el)}}list.appendChild(el)})}
function renderGalleryAdmin(el){const box=el.querySelector('[data-gallery-preview]');if(!box)return;const vals=el.querySelector('[data-field="images"]').value.split('\n').filter(Boolean);box.innerHTML=vals.map((u,j)=>`<div class="ga-thumb" style="background-image:url('${u}')"><button data-remove-photo="${j}">×</button></div>`).join('');box.style.cssText='display:grid;grid-template-columns:repeat(auto-fill,minmax(100px,1fr));gap:8px;margin:10px 0';box.querySelectorAll('[data-remove-photo]').forEach(btn=>btn.onclick=()=>{const arr=el.querySelector('[data-field="images"]').value.split('\n').filter(Boolean);arr.splice(Number(btn.dataset.removePhoto),1);el.querySelector('[data-field="images"]').value=arr.join('\n');renderGalleryAdmin(el)});box.querySelectorAll('.ga-thumb').forEach(x=>x.style.cssText+=';height:90px;background-size:cover;background-position:center;border-radius:12px;position:relative')}
function collectBlocks(){const s=data.sections.find(x=>x.id===editingSectionId),orig=s.blocks||[];return[...document.querySelectorAll('#blockEditorList .block-edit')].map((el,i)=>{const type=orig[i]?.type||'text',obj={type};el.querySelectorAll('[data-field]').forEach(f=>{if(f.dataset.field==='images')obj.images=f.value.split('\n').filter(Boolean);else if(f.dataset.field==='columns')obj.columns=Number(f.value);else obj[f.dataset.field]=f.value});return obj})}
function persistBlocksFromEditor(){const s=data.sections.find(x=>x.id===editingSectionId);if(s)s.blocks=collectBlocks()}function moveBlock(i,d){persistBlocksFromEditor();const s=data.sections.find(x=>x.id===editingSectionId),j=i+d;if(j<0||j>=s.blocks.length)return;[s.blocks[i],s.blocks[j]]=[s.blocks[j],s.blocks[i]];renderBlockEditor(s.blocks)}function removeBlock(i){persistBlocksFromEditor();const s=data.sections.find(x=>x.id===editingSectionId);s.blocks.splice(i,1);renderBlockEditor(s.blocks)}
document.querySelectorAll('[data-add-block]').forEach(btn=>btn.onclick=()=>{persistBlocksFromEditor();const s=data.sections.find(x=>x.id===editingSectionId),t=btn.dataset.addBlock,b=t==='image'?{type:'image',url:'',caption:''}:t==='gallery'?{type:'gallery',images:[],columns:2}:{type:t,text:''};s.blocks.push(b);renderBlockEditor(s.blocks)});
$('saveSection').onclick=()=>{const s=data.sections.find(x=>x.id===editingSectionId);if(!s)return;s.blocks=collectBlocks();s.label=$('secLabel').value;s.icon=$('secIcon').value;s.title=$('secTitle').value;s.subtitle=$('secSubtitle').value;s.opacity=Number($('secOpacity').value);s.overlay=Number($('secOverlay').value);saveData();$('sectionEditor').close()};$('deleteSection').onclick=()=>{if(!confirm('Delete this room?'))return;data.sections=data.sections.filter(x=>x.id!==editingSectionId);saveData();$('sectionEditor').close()};

$('exportData').onclick=()=>{const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='sophie-wonderland-content.json';a.click();URL.revokeObjectURL(a.href)};$('importData').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const incoming=migrate(JSON.parse(await f.text()));if(!Array.isArray(incoming.sections))throw new Error();data=incoming;saveData();hydrateGeneral();hydrateThemeAssets();alert('Imported!')}catch{alert('That file does not look like Wonderland data.')}};$('resetData').onclick=()=>{if(confirm('Reset all local edits to demo content?')){data=clone(demoData);saveData();hydrateGeneral();hydrateThemeAssets()}};

applyTheme();renderHub();renderFinal();
